syntax = "proto3";

package data;

service IPService {
    rpc StreamData (stream Request) returns (stream Response) {}
}

message Request {
    string request = 1;
}

message Response {
    string response = 1;
}